/**
 * 
 * 
 * 1. jwt = json web token
 * 2. generate a token by using jwt.sign
 * 3. create api set to cookie. httpOnly, secure and same-site
 * 4. from client side: axios withCredentials: true
 * 5. cors setup origin and credentials
 * 
 * 
 */